# Auth Flow Screenshots

This folder tracks auth screen progression by version.

- `v1/` contains the original auth screenshots.
- `v2/` is the next pass, based on the copy, casing, and UX fixes in `v2/README.md`.

